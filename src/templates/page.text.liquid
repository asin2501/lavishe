<div class="text-page">
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-xl-10">
                <h2 class="text-page__title">{{ page.title }}</h2>
                {{ page.content }}
            </div>
        </div>
    </div>
</div>